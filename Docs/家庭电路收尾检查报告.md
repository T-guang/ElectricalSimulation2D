# 家庭电路收尾检查报告

检查日期：2026-06-02

## 自动检查结果

### 1. Git 状态与范围

当前未提交的工业相关临时内容未纳入本次家庭电路稳定版提交：

- `Assets/Resources/Blueprints/Templates/motor_jog_control_template.json`
- `Assets/Scripts/Editor/PaletteUpdater.cs`
- `run_update.bat`

本次家庭收尾只处理练习参考面板提示简化，并记录自动检查结果。

### 2. 家庭标准模板静态校验

`template_catalog.json` 中家庭电路模板数量：8。

已静态校验以下模板：

1. 单开单控照明电路
2. 双控照明电路
3. 空气开关控制照明电路
4. 单相电能表照明电路
5. 电灯泡与电风扇并联控制电路
6. 单开控制双灯电路
7. 双开分别控制双灯电路
8. 空开控制灯泡与风扇并联电路

校验内容：

- 模板 JSON 文件存在
- JSON 可解析
- `definitionName` 能在 `Assets/Data` 中找到
- 导线引用的 `terminalId` 能在对应元件定义中找到
- 未发现重复 `instanceId`

结果：通过。

### 3. 家庭元件端子清单

- `AC_220V_Power`：L、N
- `Breaker_2P`：P1_IN、P1_OUT、P2_IN、P2_OUT
- `Single_Control_Switch`：L、L1
- `Two_Way_Switch`：L、L1、L2
- `Single_Phase_Meter`：L_IN、L_OUT、N_IN、N_OUT
- `Lamp_220V`：L、N
- `Fan_220V`：L、N

结果：家庭模板引用端子与定义一致。

### 4. 练习反馈范围检查

新网表判定反馈输出范围保持为：

- 缺少元件
- 多余元件
- 缺少连接
- 端子接错
- 多余连接
- 正确连接建议

未发现评分输出作为主反馈。

双控照明 V1 仍按标准模板严格判定，暂不处理 L1/L2 对调等价端子规则。

### 5. 练习参考面板提示简化

已将练习参考面板中的长说明简化为只显示当前图纸名称，避免显示练习目标、推荐元件和操作提示等大段文字。

### 6. 编译检查

使用临时 C# 工程执行编译检查。

结果：

- 0 compiler error
- 1 个已有 warning：`WorkspaceController.simulationDirty` 已赋值但未使用

## 需要人工在 Unity 中继续确认的项目

以下内容无法仅靠静态脚本完全确认，需要在 Unity Game 视图中点测：

### 1. 8 个家庭模板加载

逐个点击“加载图纸”，确认：

- 模板显示在家庭电路分类
- 名称、难度、描述正常
- 画布非空时弹出确认
- 取消不清空画布
- 确认后加载成功
- 元件与导线布局基本合理
- Console 无错误

### 2. 3 个网表判定练习

需要手动接线并提交检测：

- 单开单控照明电路
- 单开控制双灯电路
- 双控照明电路

重点确认：

- 正确接线通过
- 缺线能提示缺少连接
- 绕过开关能提示错误连接或多余连接
- 双灯电路不再把多个同类元件误判为问题

### 3. 图纸集进入练习流程

需要确认：

- 第一次进入配置，确认后立即切到模拟电路
- 取消时不清空、不切换
- 进入练习后参考面板只显示图纸名称
- 提交练习检测有反馈
- 退出练习后 `IsPracticeActive` 结束

### 4. 参数与万用表

需要确认：

- 修改电源电压后，测量显示跟随变化
- 修改灯泡功率后，电流跟随变化
- 修改风扇功率后，测量显示跟随变化
- 保存并导入后参数恢复

### 5. 保存 / 导入 / 删除用户图纸

需要确认：

- 保存用户图纸正常
- 同名覆盖确认正常
- 导入用户图纸正常
- 删除用户图纸正常
- 系统模板不出现在用户图纸删除列表
- 用户图纸不出现在标准模板面板

### 6. 自由接线与仿真

需要确认：

- 自由拖元件正常
- 自由接线正常
- 导线拖动正常
- 删除导线正常
- 撤销 / 重做正常
- 开始 / 结束仿真正常
- 万用表和参数面板正常

## 阶段结论

自动检查部分已通过，家庭电路 V1 具备进入人工收尾验收的条件。

建议在人工点测通过后，将当前节点作为“家庭电路稳定版”提交。