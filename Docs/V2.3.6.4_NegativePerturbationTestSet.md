# V2.3.6.4 结构级接线校验负向扰动测试集

文档版本：V2.3.6.4 初版回填  
适用阶段：V2.3.6.1 ～ V2.3.6.3  
用途：结构级接线校验人工回归基线  
当前状态：已回填历史验收结果，部分用例仍待后续复测  
说明：本轮仅整理文档，不修改运行代码和模板。

## 1. 测试集目标

本测试集用于固定 V2.3.6.1 至 V2.3.6.3 已支持的接线校验规则，作为后续结构级接线校验、参数估算和运行逻辑修改前后的人工回归基线。

目标：

- 正常模板不误报。
- 每条已支持 RuleId 都有正向触发用例。
- 每条已支持 RuleId 都有防误报用例。
- 复杂模板不被错误归类为其它结构错误。
- 已知边界明确记录，避免把未支持能力误判为回归失败。
- 后续可逐步迁移为 Editor Test 或 PlayMode Test。

本测试集不引入新规则，不改变运行层，不改变模板 JSON，不改变参数估算公式。

## 2. 测试状态说明

| 状态 | 含义 |
| --- | --- |
| 待测 | 尚未执行或本轮未确认。 |
| 历史验收通过 | 此前开发过程中已手动验收通过，本轮仅回填记录。 |
| 通过 | 本轮实际重新执行并通过。 |
| 失败 | 实际结果与期望不一致。 |
| 已知边界 | 当前版本设计边界内的行为，不作为失败。 |
| 待复测 | 历史上通过或部分验证过，但建议后续封版前再抽查。 |
| 跳过 | 当前版本不支持或不适用该场景。 |

本轮未重新执行完整手动测试，仅将前序开发与验收过程中已经确认过的结果回填到本文档。

## 3. 当前已支持 RuleId 列表

| RuleId | Severity | 含义 | 当前边界 |
| --- | --- | --- | --- |
| `STAR_DELTA_PARTIAL_STARPOINT_SHORT` | Error | 星三角电机 `U2/V2/W2` 存在局部静态直接短接。 | 只检测用户直接导线静态短接，不穿过接触器、KT、按钮、空开、熔断器、热继、端子排或运行态触点。 |
| `STAR_DELTA_INPUT_TERMINAL_SHORT` | Error | 星三角电机 `U1/V1/W1` 输入端存在静态直接短接。 | 只检测输入端三相之间的直接导线短接，不把标准星三角支路误判为输入端短接。 |
| `STOP_BUTTON_BYPASSED` | Error | 停止按钮断开后，控制线圈仍然得电。 | 第一版只支持唯一普通停止按钮 `Button_Stop_NC`，多停止按钮场景跳过。 |
| `THERMAL_RELAY_CONTROL_BYPASSED` | Error | 热继 `95/96` 断开后，对应上游 KM 仍然得电。 | 必须通过热继保护范围绑定 helper 可靠建立 `FR -> KM -> Motor`，不可靠时跳过。 |
| `SELF_HOLDING_BRANCH_INCOMPLETE` | Warning | 检测到 KM `13/14` 疑似用于自锁，但没有形成有效启动按钮并联保持支路。 | 只在 `13/14` 至少一侧有接线但未形成有效并联时提示；`13/14` 全部删除时不报。 |
| `REVERSING_CONTACTOR_CONFLICT` | Error | 同一台电机的正反转接触器出现互锁冲突或同时吸合风险。 | 必须依赖 `ReversingPairScopeHelper` 识别可靠正反转 pair；不扫描任意两个 KM。 |

## 4. 历史验收摘要

截至 V2.3.6.4 初版回填，以下规则已有手动历史验收通过：

- `STAR_DELTA_PARTIAL_STARPOINT_SHORT`
- `STAR_DELTA_INPUT_TERMINAL_SHORT`
- `STOP_BUTTON_BYPASSED`
- `THERMAL_RELAY_CONTROL_BYPASSED`
- `SELF_HOLDING_BRANCH_INCOMPLETE`
- `REVERSING_CONTACTOR_CONFLICT`

其中：

- `SELF_HOLDING_BRANCH_INCOMPLETE` 的 `13/14` 全删场景记录为已知边界。
- `THERMAL_RELAY_CONTROL_BYPASSED` 触发时可能叠加 `MOTOR_MISSING_PHASE`，记录为已知边界，不作为失败。
- `REVERSING_CONTACTOR_CONFLICT` 不依赖两个 KM 最终同时得电，而读取互锁冲突证据。
- 部分跨模板正向触发用例仍待复测。

## 5. 正常模板回归清单

### 5.1 家庭模板

| 编号 | 模板名称 | 操作状态 | 期望结果 | 不应出现的 RuleId | 实测结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| H-01 | 单开单控照明 | 正常通断 | 无结构级接线错误 | 全部结构级 RuleId | 历史验收通过 | 家庭 8 张模板整体防误报历史验收通过。 |
| H-02 | 双控照明 | 正常通断 | 无结构级接线错误 | 全部结构级 RuleId | 历史验收通过 | 家庭 8 张模板整体防误报历史验收通过。 |
| H-03 | 空气开关控制照明 | 空开 ON/OFF | 无结构级接线错误 | 全部结构级 RuleId | 历史验收通过 | 家庭 8 张模板整体防误报历史验收通过。 |
| H-04 | 单相电能表照明 | 正常供电 | 无结构级接线错误 | 全部结构级 RuleId | 历史验收通过 | 家庭 8 张模板整体防误报历史验收通过。 |
| H-05 | 灯泡 + 风扇并联 | 正常运行 | 无结构级接线错误 | 全部结构级 RuleId | 历史验收通过 | 家庭 8 张模板整体防误报历史验收通过。 |
| H-06 | 单开控制双灯 | 正常通断 | 无结构级接线错误 | 全部结构级 RuleId | 历史验收通过 | 家庭 8 张模板整体防误报历史验收通过。 |
| H-07 | 双开分别控制双灯 | 正常通断 | 无结构级接线错误 | 全部结构级 RuleId | 历史验收通过 | 家庭 8 张模板整体防误报历史验收通过。 |
| H-08 | 空开控制灯泡 + 风扇并联 | 空开 ON/OFF | 无结构级接线错误 | 全部结构级 RuleId | 历史验收通过 | 家庭 8 张模板整体防误报历史验收通过。 |

### 5.2 工业模板

| 编号 | 模板名称 | 操作状态 | 期望结果 | 不应出现的 RuleId | 实测结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| I-01 | 点动控制 | 启动按钮按下/松开 | 正常点动，不误报自锁缺失 | `SELF_HOLDING_BRANCH_INCOMPLETE`, `STOP_BUTTON_BYPASSED`, `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 点动无自锁是正常结构。 |
| I-02 | 连续运行 | 启动、停止、自锁保持 | 正常运行，无结构级错误 | 全部结构级 RuleId | 历史验收通过 | 连续运行正常自锁与停止逻辑已回归。 |
| I-03 | 热继保护 | FR ON 正常运行，FR OFF 正常失电 | 无热继旁路错误 | `THERMAL_RELAY_CONTROL_BYPASSED` | 历史验收通过 | 热继旁路与防误报均已手动验收。 |
| I-04 | 正反转 | 正转/反转单方向运行 | 无正反转冲突 | `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 普通正反转正常只吸合一个方向时不报。 |
| I-05 | 点动 + 连续 | 点动与连续模式切换 | 无结构级误报 | `SELF_HOLDING_BRANCH_INCOMPLETE`, `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 第一版自锁规则保守跳过复杂结构。 |
| I-06 | 按钮 + 接触器双重联锁正反转 | 正转/反转单方向运行 | 无正反转冲突 | `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 正常运行防误报已通过；跨接触发仍待复测。 |
| I-07 | 电气互锁正反转 | 正转/反转单方向运行 | 无正反转冲突 | `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 正常运行防误报已通过；跨接触发仍待复测。 |
| I-08 | 自动往返 | 正向/反向自动切换 | 无结构级误报 | `REVERSING_CONTACTOR_CONFLICT`, `SELF_HOLDING_BRANCH_INCOMPLETE` | 待复测 | 本轮历史回填未确认正反转冲突专项下的自动往返防误报。 |
| I-09 | 两电机时间继电器顺序启动 | M1/M2 顺序运行 | 无正反转冲突 | `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 两个 KM 同时得电不等于正反转。 |
| I-10 | 星三角降压启动 | 停止、星形、三角阶段 | 无端子不变量误报 | `STAR_DELTA_PARTIAL_STARPOINT_SHORT`, `STAR_DELTA_INPUT_TERMINAL_SHORT` | 历史验收通过 | 停止、星形、三角阶段均已回归。 |

## 6. 元件级不变量负向用例

### 6.1 星三角 U2/V2/W2 局部短接

测试对象：`Motor_StarDelta_380V`

| 编号 | 扰动方式 | 期望 RuleId | Severity | 不应出现 | 估算期望 | 实测结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SD-S-01 | `U2 - V2` 静态直接接线 | `STAR_DELTA_PARTIAL_STARPOINT_SHORT` | Error | `STAR_DELTA_INPUT_TERMINAL_SHORT` | 不输出正常星三角电流估算 | 历史验收通过 | V2.3.6.2 修正版已验收：U2/V2/W2 任意两端静态直接短接均报 `STAR_DELTA_PARTIAL_STARPOINT_SHORT`。 |
| SD-S-02 | `V2 - W2` 静态直接接线 | `STAR_DELTA_PARTIAL_STARPOINT_SHORT` | Error | `STAR_DELTA_INPUT_TERMINAL_SHORT` | 不输出正常星三角电流估算 | 历史验收通过 | V2.3.6.2 修正版已验收：U2/V2/W2 任意两端静态直接短接均报 `STAR_DELTA_PARTIAL_STARPOINT_SHORT`。 |
| SD-S-03 | `U2 - W2` 静态直接接线 | `STAR_DELTA_PARTIAL_STARPOINT_SHORT` | Error | `STAR_DELTA_INPUT_TERMINAL_SHORT` | 不输出正常星三角电流估算 | 历史验收通过 | V2.3.6.2 修正版已验收：U2/V2/W2 任意两端静态直接短接均报 `STAR_DELTA_PARTIAL_STARPOINT_SHORT`。 |

### 6.2 星三角 U1/V1/W1 输入端短接

| 编号 | 扰动方式 | 期望 RuleId | Severity | 不应出现 | 估算期望 | 实测结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SD-I-01 | `U1 - V1` 静态直接接线 | `STAR_DELTA_INPUT_TERMINAL_SHORT` | Error | `STAR_DELTA_PARTIAL_STARPOINT_SHORT` | 不输出正常星三角电流估算 | 历史验收通过 | V2.3.6.2 修正版已验收：U1/V1/W1 任意两端静态直接短接均报 `STAR_DELTA_INPUT_TERMINAL_SHORT`。 |
| SD-I-02 | `V1 - W1` 静态直接接线 | `STAR_DELTA_INPUT_TERMINAL_SHORT` | Error | `STAR_DELTA_PARTIAL_STARPOINT_SHORT` | 不输出正常星三角电流估算 | 历史验收通过 | V2.3.6.2 修正版已验收：U1/V1/W1 任意两端静态直接短接均报 `STAR_DELTA_INPUT_TERMINAL_SHORT`。 |
| SD-I-03 | `U1 - W1` 静态直接接线 | `STAR_DELTA_INPUT_TERMINAL_SHORT` | Error | `STAR_DELTA_PARTIAL_STARPOINT_SHORT` | 不输出正常星三角电流估算 | 历史验收通过 | V2.3.6.2 修正版已验收：U1/V1/W1 任意两端静态直接短接均报 `STAR_DELTA_INPUT_TERMINAL_SHORT`。 |

### 6.3 正常星三角模板防误报

| 编号 | 状态 | 期望结果 | 不应出现的 RuleId | 实测结果 | 备注 |
| --- | --- | --- | --- | --- | --- |
| SD-N-01 | 停止状态 | 无星三角端子不变量错误，且不报星三角缺相 | `STAR_DELTA_PARTIAL_STARPOINT_SHORT`, `STAR_DELTA_INPUT_TERMINAL_SHORT`, `STAR_DELTA_MISSING_PHASE` | 历史验收通过 | 正常星三角模板停止、星形、三角阶段均已回归，不再误报静态端子短接。 |
| SD-N-02 | 星形阶段 | 正常星形启动说明 | `STAR_DELTA_PARTIAL_STARPOINT_SHORT`, `STAR_DELTA_INPUT_TERMINAL_SHORT` | 历史验收通过 | 正常星三角模板停止、星形、三角阶段均已回归，不再误报静态端子短接。 |
| SD-N-03 | 三角阶段 | 正常三角运行说明 | `STAR_DELTA_PARTIAL_STARPOINT_SHORT`, `STAR_DELTA_INPUT_TERMINAL_SHORT` | 历史验收通过 | 正常星三角模板停止、星形、三角阶段均已回归，不再误报静态端子短接。 |

## 7. 结构级不变量负向用例

### 7.1 停止按钮旁路

RuleId：`STOP_BUTTON_BYPASSED`

| 编号 | 模板 | 扰动方式 | 操作步骤 | 期望结果 | 实测结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| SB-01 | 连续运行 | 停止按钮 OFF 后，跨接停止按钮前端或三相电源 `L1` 至 KM `A1`，使 KM 仍得电。 | 1. 加载模板；2. 启动并确认 KM 可运行；3. 停止按钮 OFF；4. 添加跨接线；5. 检查面板。 | 报 `STOP_BUTTON_BYPASSED`，Severity = Error；文案说明停止按钮断开但控制线圈仍得电。 | 历史验收通过 | V2.3.6.3 `STOP_BUTTON_BYPASSED` 已手动验收并提交。 |
| SB-02 | 连续运行 | 无扰动，停止按钮 OFF 后 KM 正常失电。 | 停止按钮 OFF 后检查。 | 不报 `STOP_BUTTON_BYPASSED`。 | 历史验收通过 | V2.3.6.3 `STOP_BUTTON_BYPASSED` 已手动验收并提交。 |

防误报：

- 点动模板正常不报。
- 连续运行正常不报。
- 热继保护正常不报。
- 家庭模板不报。
- 多停止按钮复杂自由接线第一版跳过，不报。

### 7.2 热继 95/96 控制保护旁路

RuleId：`THERMAL_RELAY_CONTROL_BYPASSED`

| 编号 | 模板 | 扰动方式 | 操作步骤 | 期望结果 | 实测结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| FR-01 | 热继保护 | FR OFF 后跨接控制回路，使对应 KM 仍得电。 | 1. 加载热继保护模板；2. 令 FR OFF；3. 跨接 95/96 或绕过热继控制触点；4. 检查面板。 | 报 `THERMAL_RELAY_CONTROL_BYPASSED`，Severity = Error；文案说明 FR 95/96 已断开但对应 KM 仍得电。 | 历史验收通过 | V2.3.6.3 `THERMAL_RELAY_CONTROL_BYPASSED` 已手动验收。触发时可能叠加 `MOTOR_MISSING_PHASE`，记录为已知边界，不作为失败。 |
| FR-02 | 热继保护 | FR ON 正常运行。 | 启动运行并检查。 | 不报 `THERMAL_RELAY_CONTROL_BYPASSED`。 | 历史验收通过 | V2.3.6.3 `THERMAL_RELAY_CONTROL_BYPASSED` 已手动验收。 |
| FR-03 | 热继保护 | FR OFF 后 KM 正常失电。 | FR OFF 后检查。 | 不报 `THERMAL_RELAY_CONTROL_BYPASSED`。 | 历史验收通过 | V2.3.6.3 `THERMAL_RELAY_CONTROL_BYPASSED` 已手动验收。 |
| FR-04 | 两条并联热继保护支路 | FR1 OFF，KM1 失电，KM2 正常得电。 | 构造或加载双支路自由接线后检查。 | 不误报 FR1 旁路。 | 历史验收通过 | V2.3.6.3 `THERMAL_RELAY_CONTROL_BYPASSED` 已手动验收；只检查绑定到 FR1 的 KM1。 |

补充防误报：

- 主回路断线只报缺相，不应误报热继旁路。
- 无热继模板不报。
- 家庭模板不报。
- 修改 `settingCurrent` 不触发本规则。

### 7.3 自锁支路不完整

RuleId：`SELF_HOLDING_BRANCH_INCOMPLETE`

| 编号 | 模板 | 扰动方式 | 操作步骤 | 期望结果 | 实测结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| SH-01 | 连续运行 | 保留 KM `13` 或 `14` 一侧接线，断开另一侧，使 `13/14` 没有形成启动按钮 `23/24` 两侧的有效并联。 | 1. 加载连续运行模板；2. 断开其中一侧自锁线；3. 检查面板。 | 报 `SELF_HOLDING_BRANCH_INCOMPLETE`，Severity = Warning；不是 Error。 | 历史验收通过 | 断开 KM `13/14` 其中一侧时已验收：提示 Warning，不改变运行层。 |
| SH-02 | 连续运行 | 删除 KM `13` 和 `14` 两根自锁线。 | 检查面板。 | 第一版可不报。 | 已知边界 | `13/14` 全部删除时自由接线可退化为点动控制，当前版本设计为不报错；若未来练习题明确要求连续运行，应由练习目标比对层提示。 |

防误报：

- 点动模板不报。
- 热继保护模板正常不报。
- 正反转模板不报。
- 点动 + 连续模板不报。
- 星三角模板不报。
- 家庭模板不报。

### 7.4 正反转接触器冲突

RuleId：`REVERSING_CONTACTOR_CONFLICT`

| 编号 | 模板 | 扰动方式 | 操作步骤 | 期望结果 | 实测结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| RV-01 | 正反转 | 正转运行后，从控制电源 `L1` 直接跨接到反转 KM `A1`，触发运行层互锁冲突。 | 1. 加载正反转模板；2. 让正转 KM 运行；3. 添加跨接线；4. 检查面板。 | 报 `REVERSING_CONTACTOR_CONFLICT`，Severity = Error；文案说明出现互锁冲突，运行层已阻止危险状态继续传播。 | 历史验收通过 | 普通正反转模板已完成正向触发。 |
| RV-02 | 电气互锁正反转 | 同类跨接触发互锁冲突。 | 运行一个方向后跨接另一方向 KM `A1`。 | 报 `REVERSING_CONTACTOR_CONFLICT`。 | 待复测 | 普通正反转模板已完成正向触发；电气互锁模板正常运行防误报已通过，跨接触发待后续复测。 |
| RV-03 | 双重联锁正反转 | 同类跨接触发互锁冲突。 | 运行一个方向后跨接另一方向 KM `A1`。 | 报 `REVERSING_CONTACTOR_CONFLICT`。 | 待复测 | 普通正反转模板已完成正向触发；双重联锁模板正常运行防误报已通过，跨接触发待后续复测。 |

防误报：

- 普通正反转正常只运行一个方向时不报。
- 电气互锁正反转正常不报。
- 按钮 + 接触器双重联锁正反转正常不报。
- 两电机顺序启动两个 KM 同时得电不报。
- 星三角星形阶段 KM + KMY 同时得电不报。
- 点动 + 连续不报。
- 热继保护不报。
- 家庭模板不报。

## 8. 防误报专项清单

| 编号 | 场景 | 当前状态 | 不应出现的 RuleId | 实测结果 | 备注 |
| --- | --- | --- | --- | --- | --- |
| FP-01 | 点动模板 | 启动按钮按下/松开 | `SELF_HOLDING_BRANCH_INCOMPLETE`, `REVERSING_CONTACTOR_CONFLICT`, `STOP_BUTTON_BYPASSED` | 历史验收通过 | 点动无自锁是正常。 |
| FP-02 | 连续运行模板 | 正常启动、自锁、停止 | 全部结构级 RuleId | 历史验收通过 | 正常连续运行不误报。 |
| FP-03 | 热继保护模板 | FR ON 正常运行 | `THERMAL_RELAY_CONTROL_BYPASSED` | 历史验收通过 | 热继保护模板正常不误报。 |
| FP-04 | 正反转正常模板 | 正转或反转单方向运行 | `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 正常只吸合一个方向时不报。 |
| FP-05 | 电气互锁正反转模板 | 单方向运行 | `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 正常只吸合一个方向时不报。 |
| FP-06 | 双重联锁正反转模板 | 单方向运行 | `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 正常只吸合一个方向时不报。 |
| FP-07 | 点动 + 连续模板 | 模式正常切换 | `SELF_HOLDING_BRANCH_INCOMPLETE`, `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 第一版保守跳过复杂结构。 |
| FP-08 | 两电机顺序启动模板 | KM1、KT、KM2 顺序得电 | `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | 多 KM 不等于正反转。 |
| FP-09 | 星三角模板 | 停止、星形、三角阶段 | `STAR_DELTA_PARTIAL_STARPOINT_SHORT`, `STAR_DELTA_INPUT_TERMINAL_SHORT`, `REVERSING_CONTACTOR_CONFLICT` | 历史验收通过 | KMY/KMD 不应被视为正反转 pair。 |
| FP-10 | 自动往返模板 | 正向/反向正常运行 | `REVERSING_CONTACTOR_CONFLICT`, `SELF_HOLDING_BRANCH_INCOMPLETE` | 待复测 | 本轮历史回填未确认正反转冲突专项下的自动往返防误报。 |
| FP-11 | 家庭 8 张模板 | 正常通断 | 全部结构级 RuleId | 历史验收通过 | 家庭模板不参与工业结构规则。 |
| FP-12 | 组件池拖入自由接线简单电路 | 单个电源、负载或单 KM 简单测试 | 全部不适用结构级 RuleId | 待复测 | 本轮未确认组件池自由接线简单电路专项。 |

## 9. 已知边界

1. `SELF_HOLDING_BRANCH_INCOMPLETE`
   - KM `13/14` 全部删除时不报。
   - 原因：自由接线下该结构可视为退化点动控制，不能把点动误报为自锁缺失。
   - 若未来练习题明确要求“连续运行”，应由练习目标比对层另行提示。

2. `THERMAL_RELAY_CONTROL_BYPASSED`
   - 触发时可能叠加 `MOTOR_MISSING_PHASE`。
   - 这是热继 OFF 或主回路断开带来的次生错误噪声，后续可做 Issue 优先级降噪。
   - 当前不作为阻塞。

3. `REVERSING_CONTACTOR_CONFLICT`
   - 不以两个 KM 最终同时得电作为唯一条件。
   - 运行层会阻止危险状态继续传播，因此校验层读取互锁冲突证据。
   - 必须依赖 `ReversingPairScopeHelper`，不能扫描任意两个 KM。

4. `ReversingPairScopeHelper`
   - 只支持普通三相电机正反转。
   - 不支持 `Motor_StarDelta_380V`。
   - 多台 Motor 或多个可靠 pair 存在歧义时保守返回空。
   - 不依赖实例名、显示名或模板名判断正转/反转。

5. 星三角端子不变量
   - `STAR_DELTA_PARTIAL_STARPOINT_SHORT` 与 `STAR_DELTA_INPUT_TERMINAL_SHORT` 只使用静态直接导线图。
   - 不穿过接触器、按钮、KT、热继、空开、熔断器、端子排或电机内部连接。

6. 参数计算
   - 当前保留教学估算。
   - 不做 MNA / ngspice。
   - 结构级校验 Issue 不改变参数估算公式和运行层状态。

## 10. 验收记录模板

```text
测试编号：
测试名称：
所属 RuleId：
模板：
扰动方式：
操作步骤：
期望结果：
实际结果：
是否通过：
备注：
截图路径：
提交版本：
测试日期：
测试人：
```

## 11. 后续自动化建议

第一阶段保留本手动测试集，作为 V2.3.6.4 的人工回归基线。

后续可逐步把以下流程做成 Editor Test：

1. 加载模板 JSON。
2. 按用例生成扰动导线或删除指定导线。
3. 调用 `CircuitStateAnalyzer` 与 `CircuitValidationService`。
4. 断言指定 RuleId 存在或不存在。
5. 输出机器可读测试报告。

优先自动化顺序：

1. 星三角端子短接：
   - `STAR_DELTA_PARTIAL_STARPOINT_SHORT`
   - `STAR_DELTA_INPUT_TERMINAL_SHORT`
2. 停止按钮旁路：
   - `STOP_BUTTON_BYPASSED`
3. 热继旁路：
   - `THERMAL_RELAY_CONTROL_BYPASSED`
4. 自锁支路不完整：
   - `SELF_HOLDING_BRANCH_INCOMPLETE`
5. 正反转冲突：
   - `REVERSING_CONTACTOR_CONFLICT`

本轮不强行自动化，避免为了测试夹具引入新的运行层或模板扰动风险。
